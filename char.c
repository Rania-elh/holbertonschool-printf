#include "main.h"


/**
 * char - print a character
 * @c: character
 * Return: print a character
 */

int char( *format, *c,...)
{
if (format == NULL)
       return (NULL);
if (format == 'c')
	return 
